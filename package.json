{
  "name": "rgrid",
  "version": "0.9.2",
  "private": true,
  "description": "A static browser spreadsheet with R formulas evaluated by webR",
  "type": "module",
  "scripts": {
    "check": "python tests/static_check.py && node --check app.js && python -m py_compile serve.py tests/*.py",
    "test": "npm run check",
    "test:browser": "python tests/browser_smoke.py"
  }
}
